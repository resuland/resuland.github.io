---
export interface Props {
  className?: string
}

const { className = '' } = Astro.props
---

<section class={`${className} cv-sec`}>
  <slot />
</section>

<style>
  .cv-sec {
    @apply flex;
    @apply flex-col;
  }
</style>
