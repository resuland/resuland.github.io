---
export interface Props {
  className?: string
  title: string
}

const { className = '', title } = Astro.props
---

<div class={`${className} cv-sub-sec-title`}>
  <slot />
  <span>{title}</span>
</div>

<style>
  .cv-sub-sec-title {
    @apply flex;
    @apply flex-wrap;
    @apply items-center;
    @apply text-lg;
    @apply font-semibold;
  }
</style>
